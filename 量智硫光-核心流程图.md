# 量智硫光核心流程图

本文档整理“量智硫光”项目当前最关键的三张图：

1. 项目总流程图
2. 化学到量子的转换流程图
3. 系统总体架构图

可直接用于方案讨论、答辩梳理和后续 PPT 绘制。

## 一、项目总流程图

```mermaid
flowchart LR
    A["候选催化材料库"] --> B["经典粗筛"]
    B --> C["筛出少量关键候选"]
    C --> D["构建活性位微模型"]
    D --> E["生成锂硫关键中间体构型"]
    E --> F["电子结构问题构建"]
    F --> G["量子化学线路生成"]
    G --> H["分布式量子编译"]
    H --> I["模拟执行与能量评估"]
    I --> J["化学指标计算"]
    H --> K["分布式指标计算"]
    J --> L["综合评分"]
    K --> L
    L --> M["候选排序与推荐输出"]
```

### 图示说明

这张图回答的是“项目最终怎么得到筛选结果”。

核心逻辑不是直接用分布式系统筛材料，而是：

1. 先用经典方法缩小候选范围。
2. 再对关键候选做量子化学建模和分布式量子精修。
3. 最终融合化学结果和分布式部署结果完成推荐。

## 二、化学到量子的转换流程图

```mermaid
flowchart TD
    A["候选催化位 + Li2Sx 中间体"] --> B["确定原子坐标与吸附构型"]
    B --> C["经典量子化学前处理"]
    C --> D["单/双电子积分计算"]
    D --> E["费米子哈密顿量构建"]
    E --> F["活性空间选择"]
    F --> G["量子比特哈密顿量映射"]
    G --> H["Hartree-Fock 初态构建"]
    H --> I["参数化 Ansatz 构建"]
    I --> J["基础门分解"]
    J --> K["导出量子线路骨架/QASM"]
    K --> L["进入分布式划分与映射模块"]
```

### 图示说明

这张图回答的是“化学问题到底如何变成量子线路”。

最重要的几点是：

1. 输入不是材料名字，而是具体微模型和原子构型。
2. 真正进入量子系统之前，先要经过电子结构建模和活性空间裁剪。
3. 分布式系统接收的不是化学方程式，而是已经分解好的量子线路骨架。

## 三、系统总体架构图

```mermaid
flowchart TB
    subgraph UI["前端展示层"]
        U1["材料筛选工作台"]
        U2["流程进度与任务管理"]
        U3["榜单/图表/分区可视化"]
    end

    subgraph API["后端协调层"]
        A1["任务编排与状态轮询"]
        A2["实验配置接口"]
        A3["结果聚合与导出"]
    end

    subgraph CHEM["化学建模层"]
        C1["候选材料与活性位模板"]
        C2["构型生成"]
        C3["电子结构与活性空间处理"]
    end

    subgraph QUANT["量子求解层"]
        Q1["哈密顿量映射"]
        Q2["HF 初态 + Ansatz"]
        Q3["VQE 线路生成"]
        Q4["模拟执行与能量评估"]
    end

    subgraph DIST["分布式编译层"]
        D1["线路划分"]
        D2["芯片映射"]
        D3["跨 QPU 路由优化"]
        D4["部署指标统计"]
    end

    subgraph SCORE["决策评分层"]
        S1["ChemScore 化学性能分"]
        S2["DeployScore 部署分"]
        S3["FinalScore 综合推荐分"]
    end

    U1 --> A2
    U2 --> A1
    U3 --> A3

    A2 --> C1
    C1 --> C2
    C2 --> C3
    C3 --> Q1
    Q1 --> Q2
    Q2 --> Q3
    Q3 --> D1
    D1 --> D2
    D2 --> D3
    D3 --> D4
    Q3 --> Q4
    D3 --> Q4
    Q4 --> S1
    D4 --> S2
    S1 --> S3
    S2 --> S3
    S3 --> A3
    A3 --> U3
```

### 图示说明

这张图回答的是“系统里每一层各自负责什么”。

其中有两个关键关系需要强调：

1. 分布式编译层不是单独的求解器，它负责部署和优化。
2. 真正的筛选结果来自“化学性能分 + 分布式部署分”的联合决策。

## 四、答辩时的推荐讲法

如果你要在答辩中讲这三张图，建议按下面顺序展开：

1. 先讲项目总流程图，说明为什么项目能输出真正的材料筛选结果。
2. 再讲化学到量子的转换流程图，说明系统不是黑箱，化学问题怎样被严格转换为量子计算任务。
3. 最后讲系统总体架构图，突出你们现有分布式量子编译系统在整个平台中的核心作用。

## 五、后续可扩展图

如果后续需要，我可以继续补下面几类图：

1. 综合评分机制图
2. 挑战杯答辩专用简化版总图
3. 前端页面信息架构图
4. 分布式量子编译子流程图

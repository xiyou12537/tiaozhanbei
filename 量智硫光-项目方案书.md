# “量智硫光”项目方案书

## 一、项目名称

量智硫光：面向锂硫电池催化材料筛选的分布式量子-经典协同计算系统

## 二、项目定位

本项目不是一个单纯的量子线路划分工具，也不是一个脱离工程约束的化学模拟概念验证，而是一个面向锂硫电池催化材料筛选场景的量子-经典协同计算平台。

项目聚焦锂硫电池中多硫化锂转化缓慢、催化材料筛选成本高、传统高精度电子结构计算扩展性不足等问题，构建“经典粗筛 + 分布式量子精修 + 综合评分决策”的完整技术链路。系统以候选催化材料活性位微模型为对象，先通过经典方法完成快速预筛，再对关键候选构建量子化学问题，并借助分布式量子编译与模拟执行能力完成高精度精修，最终输出材料综合排序与推荐结果。

## 三、项目背景与问题定义

锂硫电池具有高理论比能量和低成本优势，被认为是下一代高能量密度储能体系的重要方向。但其商业化进程长期受限于多硫化锂中间体的穿梭效应和转化动力学缓慢。催化材料可以通过调控多硫化锂吸附和转化过程改善电化学性能，但在材料研发中存在三类突出问题：

1. 候选材料空间大，实验试错成本高。
2. 传统经典方法在大体系和强关联问题上面临精度与规模的权衡。
3. 现有量子化学量子算法虽然有潜力，但单芯片量子比特容量有限，难以承载更复杂的催化反应微模型。

因此，本项目希望解决的问题不是“直接模拟整个电池体系”，而是围绕锂硫催化中的关键反应片段，建立可解释、可扩展、可部署的材料筛选流程。

## 四、项目目标

### 4.1 总体目标

构建一个面向锂硫电池催化材料筛选的分布式量子-经典协同计算系统，实现从候选材料输入、化学问题建模、量子线路生成、分布式编译、模拟执行到综合评分输出的完整闭环。

### 4.2 阶段目标

1. 跑通从化学微模型到量子线路再到分布式编译的全链路。
2. 在锂硫相关小规模模型上完成分布式量子精修与结果验证。
3. 建立兼顾化学性能与分布式可执行性的综合评分体系。
4. 形成面向挑战杯展示的可视化平台与项目方案。

## 五、核心创新点

### 5.1 场景创新

将分布式量子编译系统从通用线路处理场景，拓展到锂硫电池催化材料筛选这一具体能源化学问题，形成“应用牵引算法”的项目叙事。

### 5.2 架构创新

采用“经典粗筛 + 分布式量子精修”的协同路线，避免把量子计算直接用于全量候选搜索，将量子资源聚焦到高价值候选的关键反应片段上。

### 5.3 工程创新

将已有的量子线路划分、多芯片映射和分布式路由能力，嵌入量子化学求解流程，使量子化学线路能够在多 QPU 架构下完成部署评估和等效执行。

### 5.4 评价创新

不是仅输出线路划分指标，也不是仅输出化学能量，而是同时输出材料化学性能分、分布式部署分和综合推荐分，形成真正可用于筛选决策的双目标评价体系。

## 六、技术路线

### 6.1 总体思路

本项目采用三阶段协同求解路线：

1. 经典粗筛阶段：对候选催化材料进行低成本快速评估，缩小候选范围。
2. 量子精修阶段：对关键候选构建量子化学模型，生成参数化量子线路，并通过分布式系统完成编译与执行。
3. 决策输出阶段：融合化学结果与分布式执行结果，完成候选排序和推荐。

### 6.2 完整流程

```text
候选催化材料输入
-> 活性位微模型构建
-> 锂硫关键中间体构型生成
-> 经典粗筛
-> 电子结构问题构建
-> 活性空间选择
-> 费米子哈密顿量生成
-> 量子比特哈密顿量映射
-> Hartree-Fock 初态 + 参数化 Ansatz 构建
-> 基础门分解与线路导出
-> 分布式划分 / 映射 / 路由
-> 模拟执行与能量评估
-> 化学指标计算
-> 分布式指标计算
-> 综合评分与候选排序
```

## 七、化学到量子的建模流程

### 7.1 研究对象选择

本项目不直接模拟完整电极界面，而是选择具有代表性的“活性位微模型 + 多硫化锂中间体”作为研究对象。第一阶段建议选择以下任务作为主要展示场景：

1. `Li2Sx` 在候选活性位上的吸附强度评估。
2. `Li2S2` 或 `Li2S` 转化相关反应前后能量差评估。

### 7.2 化学问题建模

输入对象包括：

1. 候选催化位模板，如单原子位点、缺陷碳位点或简化硫化物簇位点。
2. 锂硫关键中间体，如 `Li2S4`、`Li2S2`、`Li2S`。
3. 对应的原子坐标和吸附构型。

本阶段输出的是一个明确的电子结构问题，而不是材料名称的抽象描述。

### 7.3 电子结构求解前端

对给定构型，系统需要完成：

1. 经典量子化学前处理。
2. 单电子积分和双电子积分计算。
3. 费米子哈密顿量构建。
4. 活性空间裁剪。

其中，活性空间选择是整个流程的关键步骤，它决定后续逻辑量子比特规模，也是化学合理性和量子可执行性之间的桥梁。

### 7.4 量子问题构造

将费米子哈密顿量映射为量子比特哈密顿量，优先采用 Jordan-Wigner 映射。随后构建：

1. Hartree-Fock 初态。
2. 适用于量子化学问题的参数化 Ansatz，第一阶段采用 UCCSD 或简化变体。
3. 基础门分解后的量子线路骨架。

此时得到的不是最终材料筛选结论，而是可进一步送入分布式系统处理的量子化学线路。

## 八、分布式量子编译与执行流程

### 8.1 分布式系统角色

现有系统的核心能力是量子线路划分、芯片映射与路由优化。它本质上是“分布式量子编译器”，不是单独的化学求解器。

因此，本项目中分布式模块承担以下职责：

1. 对量子化学线路进行多 QPU 划分。
2. 在给定拓扑下完成分区到芯片的映射。
3. 估计跨 QPU 通信代价和内部路由代价。
4. 生成可执行的分布式等效线路。

### 8.2 执行结果的获得方式

材料筛选必须输出可比较的化学结果，因此项目不能只停留在划分结果上。为此，系统在分布式编译之后引入模拟执行层，通过模拟器对路由后的等效线路进行执行与能量评估。

这意味着本项目的完整结果来自两条并行链路：

1. 化学结果链：输出基态能量、吸附能、反应能差等。
2. 分布式结果链：输出传态次数、SWAP 数、深度增长率、编译时间等。

最终通过综合评分机制将两类结果统一到同一个筛选界面中。

## 九、系统总体架构

### 9.1 架构分层

系统划分为五层：

1. 候选材料层：管理催化位模板、中间体和实验配置。
2. 化学建模层：完成构型输入、电子结构前处理和活性空间选择。
3. 量子求解层：完成哈密顿量映射、参数化线路生成和能量评估。
4. 分布式编译层：复用现有分区、映射、路由能力，输出部署结果。
5. 决策展示层：输出可视化结果、评分和推荐候选。

### 9.2 功能模块

建议系统包含以下模块：

1. 候选材料管理模块。
2. 化学任务配置模块。
3. 化学到量子线路转换模块。
4. 分布式编译模块。
5. 模拟执行与能量评估模块。
6. 综合评分与排序模块。
7. 可视化展示与结果导出模块。

## 十、评分体系设计

### 10.1 设计原则

评分体系必须回答两个问题：

1. 该材料化学上是否值得继续研究。
2. 该量子化学问题是否适合在分布式量子架构下精修。

因此，本项目采用“门控筛选 + 双分数 + 综合推荐”的评分机制。

### 10.2 门控规则

先对候选进行硬阈值筛除。建议门控条件包括：

1. 吸附构型是否合理。
2. 分布式执行前后结果误差是否在可接受范围内。
3. 是否能够在给定多 QPU 容量约束下成功部署。

未通过门控的候选不进入综合排序。

### 10.3 化学性能分

化学性能分 `ChemScore` 由三部分组成：

1. 吸附性能得分 `S_ads`
2. 转化活性得分 `S_rxn`
3. 稳定性得分 `S_stab`

建议采用以下结构：

```text
ChemScore = 0.50 × S_ads + 0.35 × S_rxn + 0.15 × S_stab
```

其中，吸附性能不采用“越强越好”的单调逻辑，而采用“合理窗口最优”的评分方式，以体现锂硫催化中既要抑制穿梭又要避免过强钝化的特点。

### 10.4 分布式部署分

分布式部署分 `DeployScore` 由四部分组成：

1. 结果保持性 `S_fidelity`
2. 通信代价 `S_comm`
3. 线路膨胀程度 `S_depth`
4. 求解效率 `S_eff`

建议采用以下结构：

```text
DeployScore = 0.35 × S_fidelity + 0.30 × S_comm + 0.20 × S_depth + 0.15 × S_eff
```

其中，`S_fidelity` 优先级最高，因为若分布式执行无法保持结果一致性，其余编译指标失去意义。

### 10.5 综合推荐分

对通过门控的候选，输出综合推荐分 `FinalScore`：

```text
FinalScore = 0.80 × ChemScore + 0.20 × DeployScore
```

此设计体现“材料筛选以化学性能为主、以量子部署能力为辅”的原则，更符合评审对应用导向项目的理解。

### 10.6 展示方式

系统最终应同时输出三类榜单：

1. 化学性能榜。
2. 分布式可执行性榜。
3. 综合推荐榜。

这样既能体现化学价值，也能突出项目在分布式量子执行方面的特色。

## 十一、实施方案

### 11.1 第一阶段：链路验证

目标是在小体系上验证化学到量子的完整转换和分布式编译可行性。建议使用极小模型完成：

1. 化学问题建模。
2. 参数化线路生成。
3. 分布式划分与映射。
4. 路由前后等效性验证。

### 11.2 第二阶段：锂硫相关模型接入

目标是将锂硫场景真正接入系统。建议完成：

1. `Li2S` 或 `Li2S2` 简化模型接入。
2. 小规模候选活性位模板构建。
3. 吸附任务或反应前后能量差任务跑通。

### 11.3 第三阶段：候选筛选展示

目标是形成比赛展示闭环。建议完成：

1. 预置 3 至 5 个候选催化位。
2. 对每个候选执行经典粗筛和量子精修。
3. 输出综合评分和推荐结果。
4. 在前端工作台中展示流程、指标和榜单。

## 十二、预期成果

### 12.1 技术成果

1. 一套面向锂硫催化材料筛选的量子-经典协同计算流程。
2. 一套可接入量子化学问题的分布式量子编译与执行框架。
3. 一套兼顾化学性能和分布式部署能力的材料筛选评价体系。

### 12.2 工程成果

1. 化学实验/材料筛选工作台页面。
2. 候选材料配置、任务提交、进度跟踪和结果展示功能。
3. 可视化的分区、映射、通信代价和材料榜单展示功能。

### 12.3 展示成果

1. 候选材料综合排序结果。
2. 吸附或转化相关能量指标对比图。
3. 分布式编译指标对比图。
4. 量子-经典协同筛选流程演示界面。

## 十三、项目可行性分析

### 13.1 现有基础可复用

当前项目已经具备：

1. QASM 输入与解析能力。
2. 量子线路划分能力。
3. 多芯片拓扑映射能力。
4. 异步任务管理与前后端交互能力。

因此，项目不需要从零开始搭建量子基础设施，重点是补齐化学建模、模拟执行和评分决策三部分。

### 13.2 技术风险可控

主要风险包括：

1. 化学模型过大导致量子线路规模失控。
2. 活性空间选择不合理导致结果缺乏代表性。
3. 分布式执行后结果保持误差偏大。
4. 候选评分过于主观导致说服力不足。

对应策略为：

1. 从小规模活性位微模型起步。
2. 采用逐步扩大的实验策略。
3. 优先验证等效性和结果保持性。
4. 采用双目标评分与门控机制，避免简单拍权重。

## 十四、结论

“量智硫光”项目的核心价值，不在于单独展示量子线路划分算法，也不在于空泛地宣称量子计算能够直接解决所有化学问题，而在于围绕锂硫电池催化材料筛选这一真实需求，构建一个可运行、可解释、可扩展的分布式量子-经典协同计算系统。

项目通过“经典粗筛 + 分布式量子精修”的路线，将现有分布式量子编译能力与化学求解任务结合起来，既体现量子计算在高精度模拟中的潜力，也符合当前硬件约束下的工程现实。最终，系统将输出兼顾化学性能与量子可执行性的候选材料推荐结果，为锂硫催化材料筛选提供新的技术路径。
